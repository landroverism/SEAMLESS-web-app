// Configure global settings for Element Plus message components
export const setupElementPlus = () => {
  // We'll use a different approach since ElMessage.defaults is not available
  // Instead, we'll create a custom notification service that sets the duration
  // See notification.service.js for implementation
}

export default setupElementPlus
